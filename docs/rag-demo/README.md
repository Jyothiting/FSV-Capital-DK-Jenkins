# RAG demo artifacts

| File | Description |
|------|-------------|
| `rag-ask-demo.png` | README screenshot — **Ask AI (RAG)**, retrieval excerpts + sources |
| `rag-ask-rag-mode.png` | Copy of the same RAG screenshot (backup) |
| `response-retrieval-only.json` | FAISS retrieval without OpenAI (`mode: retrieval_only`) |
| `response.json` / `response.md` | Live GPT capture — run `backend/scripts/capture_rag_demo.py` after setting `OPENAI_API_KEY` in `backend/.env.local` |
